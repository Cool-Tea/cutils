#ifndef TYPEDEF_H
#define TYPEDEF_H

#include <stdint.h>
#include <stddef.h>

typedef int32_t i32;
typedef int64_t i64;
typedef uint32_t u32;
typedef uint64_t u64;
typedef size_t usize;

typedef float f32;
typedef double f64;

#endif  // TYPEDEF_H